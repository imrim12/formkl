formkl "Your Formkl example" "This form is generated by the formkl adapter" {
	multiple "Work Experience" has {
		require text;
	}
	"Personal Information" has {
		text;
		multiple paragraph;
		multiple text as "something";
		multiple text as "something1";
		multiple text as "something2";
		multiple text as "something3";
		multiple text as "something4";
		multiple text as "something5";
		multiple text as "something6";
		multiple text as "something7";
		multiple text as "something8";
		multiple text as "something9";
		multiple text as "something10";
		multiple text as "something11";
		multiple text as "something12";
		multiple text as "something13";
		multiple text as "something14";
		multiple text as "something15";
		multiple text as "something16";
		multiple text as "something17";
		multiple text as "something18";
		multiple text as "something19";
		multiple text as "something20";
		multiple text as "something21";
		multiple text as "something22";
		multiple text as "something23";
		multiple text as "something24";
	}
	"Long section" has {
		multiple text as "something";
		multiple text as "something1";
		multiple text as "something2";
		multiple text as "something3";
		multiple text as "something4";
		multiple text as "something5";
		multiple text as "something6";
		multiple text as "something7";
		multiple text as "something8";
		multiple text as "something9";
		multiple text as "something10";
		multiple text as "something11";
		multiple text as "something12";
		multiple text as "something13";
		multiple text as "something14";
		multiple text as "something15";
		multiple text as "something16";
		multiple text as "something17";
		multiple text as "something18";
		multiple text as "something19";
		multiple text as "something20";
		multiple text as "something21";
		multiple text as "something22";
		multiple text as "something23";
		multiple text as "something24";
	}
}