{
  "name": "@formkl/shared",
  "version": "1.0.0-rc.1",
  "main": "dist/index.es.js",
  "types": "dist/types",
  "scripts": {
    "build": "rimraf ./dist && tsc && vite build ",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.8.4",
    "vite": "^3.1.3",
    "vitest": "^0.24.4"
  }
}
