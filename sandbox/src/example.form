formkl
  "Your Formkl example"
  "This form is generated by the formkl adapter"
{
  multiple "Work Experience" has {
    require text;
  }
  "Personal Information" has {
    text;
		select("A", "B", "C");
    multiple text as "something-elsse";
    multiple date;
    multiple datetime;
    multiple time;
    multiple paragraph;
    multiple text as "something";
  }
}